import { CONFIG } from '../../config/config';

export const REGISTER_URL = `${CONFIG.API_URL}/auth/register`;
export const LOGIN_URL = `${CONFIG.API_URL}/auth/login`;
export const FETCH_AUTH_USER = `${CONFIG.API_URL}/auth/current`;
